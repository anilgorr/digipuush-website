# The contact and lead forms use Netlify Forms — no environment variables or
# API keys are required. Netlify detects the form from public/__forms.html at
# deploy time and stores every submission in the Netlify dashboard (Forms tab).
#
# To be emailed each submission, add a form notification in Netlify:
#   Site settings → Forms → Form notifications → Add notification → Email.
